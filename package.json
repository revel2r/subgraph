{
  "name": "gringotts-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ revel2r/gringotts-subgraph",
    "create-local": "graph create --node http://localhost:8020/ revel2r/gringotts-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ revel2r/gringotts-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 revel2r/gringotts-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.0",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}